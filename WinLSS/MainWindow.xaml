<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinLSS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinLSS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border x:Name="AppTitleBar" Grid.Column="0" Grid.ColumnSpan="3" Padding="10,0,0,0" Background="Transparent">
            <TextBlock Text="WinLSS" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </Border>

        <Frame x:Name="Content" Grid.Row="1">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                Loading
            </TextBlock>
        </Frame>
    </Grid>
</Window>